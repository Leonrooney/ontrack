# Production Environment Variables (Render)
# Copy this file to .env when deploying to Render
# DO NOT use this for local development - use .env.local instead

# Database - Render PostgreSQL (INTERNAL connection string)
# Get this from: Render Dashboard → PostgreSQL → Connections → Internal Connection String
DATABASE_URL=postgresql://user:password@dpg-xxxxx-a.frankfurt-postgres.render.com:5432/ontrack_db?sslmode=require

# NextAuth Configuration
# Generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-generated-secret-here
NEXTAUTH_URL=https://your-app.onrender.com

# API URLs
NEXT_PUBLIC_API_URL=https://your-app.onrender.com

# Node Version (for Render builds)
NODE_VERSION=20
